# M00 — 仓库准备与分支策略

**前置**：无  
**后置**：M01  
**预估**：0.5–1 天  

---

## 1. 目标

建立空包与配置占位，使后续模块有固定落点；CI 不因 import 失败而红。

---

## 2. 任务清单

### 2.1 创建 Python 包骨架

```bash
# 在 backend/app/ 下创建（文件可为空或仅 docstring）
context_sync/__init__.py
context_sync/fetchers/__init__.py
computer_use/__init__.py
memory/token_compress.py      # M01 实现
memory/context_retrieval.py   # M02 实现
```

每个 `__init__.py` 顶部注释：`# TEO-DUAL DEV-SPEC-1.0 — see docs/development/`

### 2.2 配置项占位（`app/core/config.py`）

新增 Optional 字段（先不设默认值逻辑）：

- `context_sync_interval_sec: int = 1200`
- `computer_use_runner_url: Optional[str] = None`
- `github_oauth_client_id` / `github_oauth_client_secret`
- `google_workspace_oauth_client_id` / `google_workspace_oauth_client_secret`（可与 youtube 共用 env 名，代码层分 provider）
- `titan_sidecar_jwt_secret: Optional[str] = None`

### 2.3 Celery Beat 占位

`celery_app.py` 的 `beat_schedule` 增加注释块，M02 再取消注释：

```python
# "context-sync-tick": {"task": "titan.context_sync.tick", "schedule": 1200},
# "context-sync-rollup": {"task": "titan.context_sync.rollup", "schedule": 21600},
# "computer-use-reaper": {"task": "titan.computer_use.reaper", "schedule": 900},
```

### 2.4 catalog 占位（stub）

在 `catalog.py` 末尾追加 **status="stub"** 条目（不实现 dispatch）：

- `context_sync_run`
- `computer_use_submit` / `computer_use_status` / `computer_use_cancel`

`capability_packs.py` 增加 pack `context_sync`、`desktop_automation`（refs 指向上述 stub）。

### 2.5 分支策略

| 分支 | 用途 |
|------|------|
| `main` | 可部署 |
| `feat/teo-dual-m01` … `m07` | 按模块 PR |
| 禁止 | 单 PR 跨 M02+M03 超过 80 文件 |

PR 标题格式：`feat(teo-dual): M02 context sync gmail fetcher`

---

## 3. 验收

- [ ] `docker compose build backend` 成功
- [ ] `GET /api/v1/integrations/capabilities` 含新 stub 能力
- [ ] 无未使用 import 导致 mypy/ruff 失败（若项目已启用）

---

## 4. 不涉及

- 不建 `009` 迁移（M01）
- 不实现 OAuth 回调
